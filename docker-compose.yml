services:
  synapse-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: synapse-web
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
